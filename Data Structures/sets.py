var = set([1, 2, 3, 2, 3, 4, 4 ,4, 5, 5])
print(var)

var.add(6)

print(var)
# Sets doesn't allow duplicate values similar to the sets in math. They are mutable
# print(var[3])
# Sets are also unordered. So, we can't access the elements using indexes